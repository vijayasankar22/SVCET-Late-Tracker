# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

build:
  - fetch: .   # <— this tells Firebase to fetch code from the current directory
  - install: npm install
  - build: npm run build

runConfig:
  maxInstances: 1
